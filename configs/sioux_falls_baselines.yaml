data_dir: ./data
output_dir: /root/autodl-tmp/resilence/outputs/baselines_eval
model_dir: /root/autodl-tmp/resilence/outputs
damaged_ratio: 0.3
assignment_iters: 100
# Optional schedule (uncomment/tune if needed):
assignment_iters_start: 20
assignment_iters_end: 120
assignment_iters_warmup_episodes: 2000
assignment_method: fw
use_cugraph: false
episodes: 5000
buffer_size: 1000000
batch_start: 500
update_every: 2
updates_per_step: 4
num_workers: 16
rollout_steps_per_worker: 12
queue_size: 10000
weights_sync_every: 200
batch_size: 1024
per_alpha: 0.6
per_beta: 0.4
per_eps: 1.0e-6
reward_scale: 1.0
reward_mode: log_delta
reward_alpha: 10.0
reward_beta: 10.0
reward_gamma: 0.01
reward_clip: 50.0
capacity_damage: 1.0e-3
unassigned_penalty: 1.0e5
debug_reward: true
debug_reward_every: 10
max_steps: 150
her_ratio: 0.0
gat_layers: 3
plot_every: 1
hidden_dim: 256
embed_dim: 256
lr: 0.0003
actor_lr: 0.0001
critic_lr: 0.0003
alpha_lr: 0.00003
gamma: 0.99
target_tau: 0.001
grad_clip: 0.5
share_critic_encoder: true
seed: 42
eval_seeds: [43]
model_path: /root/autodl-tmp/resilence/outputs/baselines_eval/no_model.pt
use_torch_bpr: true
device: cuda
graphml_path: ./maps_simu/sioux_falls.graphml
alpha_max: 1.0
save_best: true
alpha_init: 0.1
